# Theme Engine

The Theme Engine connects themes, assets, site configuration and renderers.

Responsibilities:
- Active site access
- Active theme access
- Asset resolution
- Renderer support

The Theme Engine should not contain client-specific editing logic.
