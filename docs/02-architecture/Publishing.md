# Publishing

Publishing is snapshot-based.

Current publishing behavior:
- Legacy CMS snapshot is still published for backward compatibility.
- Platform WebsiteSchema is published separately as platform-published.
- WebsiteProvider can bootstrap from the platform-published snapshot.
